# FastAPI Session Management
# IMPORTANT: Generate a random secret key for production!
# You can generate one using: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-random-secret-key-minimum-32-characters-CHANGE-THIS

# Session Configuration
SESSION_MAX_AGE=14400  # 4 hours in seconds

# Cleanup Scheduler
CLEANUP_INTERVAL=3600  # Run cleanup every hour (in seconds)

# Logging Configuration
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# CORS Configuration (Optional)
# Uncomment if you need to allow specific origins
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
